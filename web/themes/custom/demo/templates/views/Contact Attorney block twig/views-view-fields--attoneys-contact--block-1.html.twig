{#
/**
 * @file
 * Default view template to display all the fields in a row.
 *
 * Available variables:
 * - view: The view in use.
 * - fields: A list of fields, each one contains:
 *   - content: The output of the field.
 *   - raw: The raw data for the field, if it exists. This is NOT output safe.
 *   - class: The safe class ID to use.
 *   - handler: The Views field handler controlling this field.
 *   - inline: Whether or not the field should be inline.
 *   - wrapper_element: An HTML element for a wrapper.
 *   - wrapper_attributes: List of attributes for wrapper element.
 *   - separator: An optional separator that may appear before a field.
 *   - label: The field's label text.
 *   - label_element: An HTML element for a label wrapper.
 *   - label_attributes: List of attributes for label wrapper.
 *   - label_suffix: Colon after the label.
 *   - element_type: An HTML element for the field content.
 *   - element_attributes: List of attributes for HTML element for field content.
 *   - has_label_colon: A boolean indicating whether to display a colon after
 *     the label.
 *   - element_type: An HTML element for the field content.
 *   - element_attributes: List of attributes for HTML element for field content.
 * - row: The raw result from the query, with all data it fetched.
 *
 * @see template_preprocess_views_view_fields()
 *
 * @ingroup themeable
 */
#}
{% if  fields.title.content  %}
<div class="col-lg-4">
  <div class="att-avatar">
    <img src="{{ fields.field_image_a.content|render|trim|striptags }}" alt="" >
  </div>
</div>
<div class="col-lg-8">
  <div class="att-title">
    <div class="att-tit-left">
      <h2>{{ fields.title.content }}</h2>
      <h5>{{ fields.field_designation.content }}</h5>
    </div>
    <div class="att-tit-right">
      <a href="#attorney-contact" class="btn btn-primary">Email Attorney</a>
    </div>
  </div>
  <div class="row att-info">
    <div class="col-sm-6">
      <div class="att-info-tit">Direct:</div>
	  {% set phone_no = fields.field_phon_.content|render %}
	  {% set phone_no = phone_no|replace({'-': ''}) %}
      <p><a href="tel:{{ phone }}">{{ fields.field_phon_.content }}</a></p>
    </div>
    <div class="col-sm-6">
      <div class="att-info-tit">FAX:</div>
      <p>
		: {{ fields.field_fax.content }}
	  </p>
    </div>
    <div class="col-sm-6">
		<div class="att-info-tit">Education:</div>
		<p>{{ fields.field_education.content|raw }}</p>
		</div>
		<div class="col-sm-6">
			<div class="att-info-tit">Bar Admissions:</div>
			<p>{{ fields.field_bar_admissions.content|raw }}</p>
			</div>
		</div>
		<ul class="att-share list-unstyled">
			<li>
			<a target="_blank" href="https://www.facebook.com">
				<img src="/themes/custom/demo/img/fb.png">
			</a>
			</li>
			<li>
			<a target="_blank" href="https://www.twitter.com">
			<img src="/themes/custom/demo/img/tweet.png">
				</a>
			</li>
			<li>
			<a target="_blank" href="https://www.linkedin.com">
				<img src="/themes/custom/demo/img/linked.png">
			</a>
			</li>				
		</ul>
</div>
{% endif %}



	